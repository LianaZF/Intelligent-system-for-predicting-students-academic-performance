## Структура папки

student_predicator/

  📂 data/ # Датсеты
  
    students.csv # Исходный датасет
    students_15000_CTGAN.csv # Увеличенный датасет (15000 записей)
    train_split.csv # Тренировочная выборка
    val_split.csv # Валидационная выборка
    test_split.csv # Тестовая выборка
  
  📂 src/ # Исходный код
  
    train.py # Основной скрипт обучения (все модели)
    catboost_only_final.py # Обучение только CatBoost
    compare_xgboost_catboost.py # Сравнение XGBoost и CatBoost
    train_regression.py # Обучение регрессионной модели (KNN)
    predict.py # Интерактивный прогноз
    server.py # Веб-сервер для API прогнозов
  
  📂 model/ # Обученные модели и результаты
  
    📂 catboost_final/ # Финальные CatBoost модели
    
      catboost_classifier.pkl # Классификатор CatBoost
      catboost_regressor.pkl # Регрессор CatBoost
      catboost_финальный_график.png
      catboost_валидация.xlsx
    
    📂 xgboost_vs_catboost_only/ # Результаты сравнения моделей
    
    📂 final_report/ # Финальные отчеты
    
      Сравнение_всех_моделей.xlsx
      График_лучшая_гипотеза.png
    
    📂 results/ # Результаты экспериментов
    
      📂 knn_regression/
      
        knn_п_прогноз_vs_реальность.png
        knn_регрессия_результаты.xlsx
    
    📂 regression/ # Регрессионные модели
    
    📂 catboost_info/ # Логи обучения CatBoost
  
  📂 GAN_dataset/ # Данные для генерации (CTGAN)

## Цель проекта

Прогнозирование успеваемости студентов на основе их активности для:
- **Классификации риска** (3 категории)
- **Регрессии точного балла** за экзамен



## Используемые признаки

| Признак | Описание | Тип данных |
|---------|----------|------------|
| `hours_studied` | Часы самостоятельной подготовки | Числовой |
| `lms_activity_hours` | Часы активности в системе LMS | Числовой |
| `attendance_percent` | Посещаемость занятий (%) | Процент |
| `previous_scores` | Средний балл за предыдущие сессии | Числовой |
| `exam_score` | итоговый балл за экзамен | Числовой |

## Модели машинного обучения

### Основные модели:
- **Random Forest** (классификация и регрессия)
- **K-Nearest Neighbors** (KNN)
- **Gaussian Naive Bayes**
- **Linear Regression**
- **Voting Ensemble** (ансамбль моделей)
- **CatBoost** (градиентный бустинг)
- **XGBoost** (градиентный бустинг)

### Проверенные гипотезы:
1. **Все 4 признака** → Лучшая точность
2. **LMS + Посещаемость** (2 признака)
3. **LMS + Посещаемость + Прошлые баллы** (3 признака)

## Цель проекта

Прогнозирование успеваемости студентов на основе их активности для:
- **Классификации риска** (3 категории)
- **Регрессии точного балла** за экзамен


## Используемые признаки

| Признак | Описание | Тип данных |
|---------|----------|------------|
| `hours_studied` | Часы самостоятельной подготовки | Числовой |
| `lms_activity_hours` | Часы активности в системе LMS | Числовой |
| `attendance_percent` | Посещаемость занятий (%) | Процент |
| `previous_scores` | Средний балл за предыдущие сессии | Числовой |



## Модели машинного обучения

### Основные модели:
- **Random Forest** (классификация и регрессия)
- **K-Nearest Neighbors** (KNN)
- **Gaussian Naive Bayes**
- **Linear Regression**
- **Voting Ensemble** (ансамбль моделей)
- **CatBoost** (градиентный бустинг)
- **XGBoost** (градиентный бустинг)

### Проверенные гипотезы:
1. **Все 4 признака** → Лучшая точность
2. **LMS + Посещаемость** (2 признака)
3. **LMS + Посещаемость + Прошлые баллы** (3 признака)

## Метрики оценки

### Для классификации:
- **Accuracy** (точность)
- **Recall для класса "риск"** (важная метрика для выявления студентов в зоне риска)

### Для регрессии:
- **R²** (коэффициент детерминации)
- **MAE** (средняя абсолютная ошибка)
- **MSE** (средняя квадратичная ошибка)

## Как использовать

### 1. Обучение моделей:
python src/train.py                    # Все модели
python src/catboost_only_final.py      # Только CatBoost
python src/compare_xgboost_catboost.py # Сравнение моделей
### 2. Прогнозирование:
python src/predict.py                  # Интерактивный режим
### 3. Запуск веб-сервера:
python src/server.py                   # API на порту 8000
### 4. Проверка регрессии:
python src/train_regression.py         # KNN регрессия


## Технические требования
Python 3.8+

### Основные библиотеки: pandas, scikit-learn, catboost, xgboost, matplotlib

### Для визуализации: openpyxl (Excel отчеты)

## Особенности реализации
Стратифицированное разбиение для сохранения баланса классов
Стандартизация признаков (StandardScaler)
Взвешивание классов для обработки дисбаланса
Ансамблирование моделей (VotingClassifier/Regressor)
Автоматическая генерация отчетов в Excel с форматированием
Визуализация результатов (графики сравнения)

## Интерфейсы
Консольный (predict.py) - для быстрой проверки
Веб-API (server.py) - для интеграции с другими системами
Excel-отчеты - для аналитики и презентаций

